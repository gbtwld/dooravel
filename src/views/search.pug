extends ./layouts/main

block content
    .search__header
        h3 Searching for: #{searchingBy}
    .search__videos
        if series.length === 0
            h5 No Series Found
        each item in series
            a(href=`${routes.series}/${item.id}`)
                h3=item.title
                p=item.description